const primaryColor = '#0d323f';

export const styles = {
  btnColor: primaryColor,
  btnStyles: {
    m: 1,
    color: primaryColor,
    border: `1px solid ${primaryColor}`,
    borderRadius: '4px',
  },
};
